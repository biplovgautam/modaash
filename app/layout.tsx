import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Modaash — Manpower Management Platform for Recruitment Agencies",
  description:
    "Streamline candidate tracking, employer relationships, and job placements. End-to-end recruitment platform built for Nepal's manpower agencies.",
  keywords: [
    "manpower management",
    "recruitment software",
    "nepal recruitment",
    "overseas employment",
    "candidate tracking",
    "workforce management",
  ],
  icons: {
    icon: "/logo.png",
    shortcut: "/logo.png",
    apple: "/logo.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
